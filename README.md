Description : Maven On The Go
==

Without the need of any maven installation, just using one runnable jar you can use maven.

Usage
==

```bash
java -jar maven-embedder-otg-<VERSION>-full.jar <MAVEN-COMMAND>
```
